# Add project-specific ProGuard rules for consumers of this library.
# Anything in this file is appended to the consumer app's proguard config
# when the consumer enables minification.
#
# Phase 0c — no rules yet. Will be populated in Phase 28 (AAR build finalize).